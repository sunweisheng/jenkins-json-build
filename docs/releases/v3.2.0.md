# v3.2.0 版本说明

发布日期：2026-08-12

## 新增

- 新增 Node/npm、Android/Gradle、React Native Android、iOS/Xcode、React Native iOS 和旧版 .NET Framework/MSBuild 模板。
- 新增 `agentServer`、`customCheckbox` 参数及旧 `multiChoice + customCheckbox` 兼容。
- Agent Server Parameter 固定为 `1.23.v3f9770f9cc1a_`，构建参数页由服务端渲染全部 Jenkins 节点，并将历史选择置顶。
- 新增参数首次初始化 `NOT_BUILT`、历史选择保留、项目全不选阻断和静态 Agent 源码检出前检查。
- 新增 `npm`、`gradle`、`msbuild`、`coverage`、`xcodebuild`、`xcodeCoverage`、`appleSigning`。
- SonarQube 增加非 Maven 脚本方式。
- V2 转换增加 npm、Gradle、MSBuild、单一 Xcode 动作、Jest/LCOV、OpenCover 和 xcresult 覆盖率。
- 新增固定摘要、非 root、无 Docker Socket、无 hostPath、无特权的多语言 Kubernetes Pod 模板。
- Node 依赖安装阶段默认在临时网络失败后重试两次；React Native Android 明确使用 Jest LCOV，避免模板继承后误用 Android JaCoCo 路径。
- Jenkins Kubernetes Cloud 默认 `containerCap` 调整为 `2`，允许同时运行两个 Agent Pod；该参数不控制节点位置，4 GB 节点必须另用标签、亲和性、污点或等效调度策略保证每台只运行一个构建 Pod。
- React Native Android 的 4 GB Node1 验收配置使用 1 GiB Gradle 堆和单工作线程所需的受限 Pod 资源；通用模板的资源默认值保持不变。

## 兼容性

- V2.1 行为保持兼容。
- Java、Maven、BuildKit、Kaniko 和 Helm 原有 V3 行为保持兼容。
- iOS 只支持 Mac Agent；旧版 .NET Framework 只支持 Windows Agent；不增加现代 `dotnet`。

## 验证状态

- 共享类库单元测试、V2 转换测试和 JSON/Pod 安全检查已通过。
- Mac Agent `mac-m2-16g` 已上线并完成工具检查、断线重连和 Jenkins Controller 重启恢复。测试环境使用 Xcode `26.6`、iOS Simulator Runtime `26.5`、OpenJDK `21.0.12`、Node.js `24.19.0`、CocoaPods `1.17.0`、Bundler `4.0.16`、Watchman `2026.07.27.00`。
- Jenkins 共享类库 47 项单元测试和 V2 转换器 5 项测试通过。
- `V32-Parameters-Acceptance #6` 验证 Custom Checkbox 全部取消后保持空值；`#7` 验证内嵌 JSON/YAML、Jenkins Home 文件、内部 HTTP、外部 HTTPS、自定义节点路径、历史选择，以及 string、boolean、choice 和 Agent Server 参数共存。
- `V32-Node-Kubernetes #9` 在 Kubernetes Agent 上通过 22 项 Jest 测试、LCOV 100% 和制品归档。
- `V32-Android-Kubernetes #7` 在 Kubernetes Agent 上通过 Gradle 测试、构建和 APK 归档。
- `V32-iOS-Static #6` 在 `mac-m2-16g` 上通过 5 项模拟器测试、xcresult 转 Cobertura、Coverage 发布和未签名 Archive。
- `V32-React-Native-iOS-Static #2` 在 `mac-m2-16g` 上通过 Jest、iOS 模拟器测试、LCOV 与 xcresult 覆盖率发布，以及未签名 Archive。
- `K8S-Deploying-Java/main #10` 通过 20 项 Maven 测试、JAR、BuildKit、GHCR、Helm、Ingress 和双副本应用部署回归。
- React Native Android 的最终真实构建结果在发布前补入本节。

## 已知未验收项

- 当前没有 Windows Agent，因此旧版 .NET Framework/MSBuild 未做真实 Windows 构建。
- 当前没有测试用 P12 和 provisioning profile，因此 Apple 真实签名、Archive 和 IPA 导出未做签名验收。
- 以上两项不阻止 v3.2.0 发布；相关代码、配置校验和清理路径由自动测试覆盖，版本说明保留真实验收状态。
